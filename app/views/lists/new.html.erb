<h2>Create a new list</h2>

<%= form_with model: @list do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>

<%= link_to lists_path do%>
  <p>Back to all lists</p>
<%end%>
